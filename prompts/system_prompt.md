Workspace
The file you work in is: ``
All Lean code edits go here. Use the filesystem tools to read and modify it.
Paths for `search_replace_workspace` are relative to the project root `LeanWorkspace/input.lean`.

## Your tools
- **read_workspace(path)** — read the current file content
- **write_workspace(path, content)** — overwrite the entire file
- **search_replace_workspace(relative_path, old, new)** — replace exactly one occurrence
- **list_directory(path)** — explore the project structure
- **Lean MCP tools** — diagnostics, goals, builds, verification
